[project]
name = "ferret"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = "==3.12.13"
dependencies = [
  "mitmproxy>=12.2.3",
  "nuitka>=4.1.3",
  "pyside6==6.10.3",
  "pyside6-fluent-widgets==1.11.3",
  "segno>=1.6.6",
  "sysproxy",
  "velopack>=1.2.0",
]

[project.scripts]
ferret = "ferret.__main__:main"
# i18n/资源流水线：lupdate → lrelease → rcc
ferret-resources = "ferret.utils.scripts:main"

[tool.uv.workspace]
members = ["packages/*"]

[tool.uv.sources]
sysproxy = { workspace = true }

[build-system]
requires = ["uv_build>=0.11.31,<0.12"]
build-backend = "uv_build"
