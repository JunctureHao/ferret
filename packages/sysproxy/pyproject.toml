[project]
name = "sysproxy"
version = "0.1.0"
description = "Ownership-aware system proxy attachment service (Windows WinINet registry)."
readme = "README.md"
requires-python = "==3.12.13"
dependencies = []

[build-system]
requires = ["uv_build>=0.11.31,<0.12"]
build-backend = "uv_build"
